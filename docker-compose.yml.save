version: '3.1'

services:
  depends_on:
      - db
  api:
    build: .
    container_name: devinsales-deploy
    restart: always
    volumes:
      - .:/usr/src/app
    ports:
      - "3333:3333"
    depends_on:
      - db
  
